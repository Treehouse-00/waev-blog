{
  "name": "waev-blog",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "blog.waev.app — news, updates, and philosophy for Waev, the opinionated MeshCore analyzer.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist --project-name waev-blog --commit-dirty=true"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
